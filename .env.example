SECRET_KEY=replace-with-a-long-random-secret-at-least-32-chars
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=replace-with-a-valid-fernet-key
# SQLite default (leave commented to use the default path)
# DATABASE_URL=sqlite:///securestock.db
# Set to True behind an HTTPS reverse proxy
SESSION_COOKIE_SECURE=False